# 更新日志

[English Changelog](CHANGELOG.md)

## 0.1.0

首次公开发布。

### 新增

- 新增 TermLens WebSSH 网关初始版本。
- 新增基于 xterm.js 的浏览器终端和基于 `ssh2` 的 Node.js SSH 后端。
- 新增后台用户、角色、SSH 目标和目标权限管理。
- 新增用户专属随机访问链接。
- 新增强制密码 + TOTP 登录和二维码设置流程。
- 新增 WebSocket 连接前的短期一次性 terminal ticket。
- 新增终端输入转发前的登录会话、访问链接、目标权限和 ticket 校验。
- 新增左侧连接面板折叠、终端布局拖拽调整和终端全屏模式。
- 新增移动端终端辅助按键和基于可见视口的终端尺寸适配。
- 新增参考 RustDesk 交互的一次性移动端修饰键，以及 `F1`-`F12` 终端辅助按键。
- 新增按系统类型显示 `Cmd`/`Win` 的终端按键标签、辅助按键排序/显隐配置，以及可发送预输入文本到当前光标位置的右侧文本输入区。
- 新增移动端终端优先布局行为，以及进入编辑、命令模式、保存退出、强制退出的 Vim 辅助操作。
- 新增可选私有终端中继模块，支持后台管理 endpoint、一次性 Agent 注册、用户私有 endpoint 权限，以及默认仅转发本机 loopback 的 Agent 安全边界。
- 新增管理后台可配置的终端空闲超时和基于活跃输入/输出的 session 续期，避免活跃终端被固定墙钟时间断开。
- 新增适配 Safari 的终端沉浸式全屏 fallback。
- 新增管理后台访问链接复制和复制并打开操作。
- 新增 systemd 和 nginx 部署示例。
- 新增英文和中文文档。
- 在文档中补充设计理念、系统架构图、交互流程图、部署步骤和使用说明。
- 新增无公网 IP 私有电脑中继访问的路线图。
- 新增安全策略和生产部署加固说明。

### 安全

- SSH 凭据只用于当前连接，不写入数据库。
- 访问链接、terminal ticket 和 session token 只以哈希形式存储。
- TOTP secret 使用 `TERMLENS_SECRET_KEY` 加密后落盘。
- 应用不持久化终端输出。
- 用户界面的 SSH 连接失败提示避免暴露后端连接细节。
